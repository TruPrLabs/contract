const now = new Date().getTime();

const delta1s = 1 * 1000;
const delta1m = 1 * 1000 * 60;
const delta1h = 1 * 1000 * 60 * 60;
const delta1d = 24 * 1000 * 60 * 60;

const delta10s = 10 * delta1s;
const delta10m = 10 * delta1m;
const delta10h = 10 * delta1h;
const delta10d = 10 * delta1d;

const delta20s = 20 * delta1s;
const delta20m = 20 * delta1m;
const delta20h = 20 * delta1h;
const delta20d = 20 * delta1d;

const delta30s = 30 * delta1s;
const delta30m = 30 * delta1m;
const delta30h = 30 * delta1h;
const delta30d = 30 * delta1d;

const delta40s = 40 * delta1s;
const delta40m = 40 * delta1m;
const delta40h = 40 * delta1h;
const delta40d = 40 * delta1d;

const delta50s = 50 * delta1s;
const delta50m = 50 * delta1m;
const delta50h = 50 * delta1h;
const delta50d = 50 * delta1d;

const future1s = now + 1 * delta1s;
const future1m = now + 1 * delta1m;
const future1h = now + 1 * delta1h;
const future1d = now + 1 * delta1d;

const future10s = now + 10 * delta1s;
const future10m = now + 10 * delta1m;
const future10h = now + 10 * delta1h;
const future10d = now + 10 * delta1d;

const future20s = now + 20 * delta1s;
const future20m = now + 20 * delta1m;
const future20h = now + 20 * delta1h;
const future20d = now + 20 * delta1d;

const future30s = now + 30 * delta1s;
const future30m = now + 30 * delta1m;
const future30h = now + 30 * delta1h;
const future30d = now + 30 * delta1d;

const future40s = now + 40 * delta1s;
const future40m = now + 40 * delta1m;
const future40h = now + 40 * delta1h;
const future40d = now + 40 * delta1d;

const future50s = now + 50 * delta1s;
const future50m = now + 50 * delta1m;
const future50h = now + 50 * delta1h;
const future50d = now + 50 * delta1d;

exports.now = String(now);

exports.delta1s = String(delta1s);
exports.delta1m = String(delta1m);
exports.delta1h = String(delta1h);
exports.delta1d = String(delta1d);

exports.delta10s = String(delta10s);
exports.delta10m = String(delta10m);
exports.delta10h = String(delta10h);
exports.delta10d = String(delta10d);

exports.delta20s = String(delta20s);
exports.delta20m = String(delta20m);
exports.delta20h = String(delta20h);
exports.delta20d = String(delta20d);

exports.delta30s = String(delta30s);
exports.delta30m = String(delta30m);
exports.delta30h = String(delta30h);
exports.delta30d = String(delta30d);

exports.delta40s = String(delta40s);
exports.delta40m = String(delta40m);
exports.delta40h = String(delta40h);
exports.delta40d = String(delta40d);

exports.delta50s = String(delta50s);
exports.delta50m = String(delta50m);
exports.delta50h = String(delta50h);
exports.delta50d = String(delta50d);

exports.future1s = String(future1s);
exports.future1m = String(future1m);
exports.future1h = String(future1h);
exports.future1d = String(future1d);

exports.future10s = String(future10s);
exports.future10m = String(future10m);
exports.future10h = String(future10h);
exports.future10d = String(future10d);

exports.future20s = String(future20s);
exports.future20m = String(future20m);
exports.future20h = String(future20h);
exports.future20d = String(future20d);

exports.future30s = String(future30s);
exports.future30m = String(future30m);
exports.future30h = String(future30h);
exports.future30d = String(future30d);

exports.future40s = String(future40s);
exports.future40m = String(future40m);
exports.future40h = String(future40h);
exports.future40d = String(future40d);

exports.future50s = String(future50s);
exports.future50m = String(future50m);
exports.future50h = String(future50h);
exports.future50d = String(future50d);
